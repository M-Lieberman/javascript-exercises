const {
  nestedArrayAndObject
} = require("../../examples/destructuring");

describe('Destructuring Tests', () => {

  test('Calculate function defines object in parameters', () => {

    // expect(nestedArrayAndObject()).toBe("Bob");
    console.log(nestedArrayAndObject());
  })

})

